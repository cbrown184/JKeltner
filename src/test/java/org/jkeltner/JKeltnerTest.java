package org.jkeltner;

import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;

import java.math.BigDecimal;

public class JKeltnerTest {

//    @Test
//    public void fivePeriodJKeltnerSma() throws JKeltnerException {
//        JKeltner keltner = new JKeltner(5, movingAverageClose, movingAverageRange);
//        Assertions.assertEquals(BigDecimal.valueOf(0), keltner.calculateSma());
//        keltner.addPrice(BigDecimal.valueOf(1));
//        Assertions.assertEquals(BigDecimal.valueOf(1), keltner.calculateSma());
//        keltner.addPrice(BigDecimal.valueOf(2));
//        Assertions.assertEquals(BigDecimal.valueOf(1.5), keltner.calculateSma());
//        keltner.addPrice(BigDecimal.valueOf(3));
//        keltner.addPrice(BigDecimal.valueOf(4));
//        keltner.addPrice(BigDecimal.valueOf(5));
//        Assertions.assertEquals(BigDecimal.valueOf(3), keltner.calculateSma());
//    }

}
